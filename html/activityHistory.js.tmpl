"use strict";

var ActivityHistory = {
    cache: {},
    cacheTTL: 300000,
    apiBaseUrl: 'https://__ROOT_PUB_DOMAIN_NAME__/api/aircraft/v2',

    hasActivity: function(icao) {
        var entry = this.cache[icao];
        return !!(entry && entry.dates && entry.dates.length > 0);
    },

    getCookie: function(name) {
        var value = '; ' + document.cookie;
        var parts = value.split('; ' + name + '=');
        if (parts.length === 2) return parts.pop().split(';').shift();
        return null;
    },

    toDateStr: function(date) {
        if (typeof date === 'string') return date;
        return date.getUTCFullYear() + '-' +
            String(date.getUTCMonth() + 1).padStart(2, '0') + '-' +
            String(date.getUTCDate()).padStart(2, '0');
    },

    fetchActiveDates: async function(icao) {
        var cached = this.cache[icao];
        if (cached && (Date.now() - cached.fetchedAt) < this.cacheTTL) {
            return cached.dates;
        }
        try {
            var response = await fetch(this.apiBaseUrl + '/operations/icao/active-dates', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                credentials: 'include',
                body: JSON.stringify({
                    user_token: this.getCookie('adsbx_api'),
                    payload: { icao: icao, time_from: 0, time_to: Math.floor(Date.now() / 1000) }
                })
            });
            if (!response.ok) return [];
            var data = await response.json();
            var dates = data.active_dates || [];
            this.cache[icao] = { dates: dates, fetchedAt: Date.now() };
            return dates;
        } catch (e) {
            return [];
        }
    },

    getNextDate: function(icao, currentDate) {
        var cached = this.cache[icao];
        if (!cached || !cached.dates || !cached.dates.length) return null;
        var current = this.toDateStr(currentDate);
        var dates = cached.dates;
        for (var i = dates.length - 1; i >= 0; i--) {
            if (dates[i] > current) return dates[i];
        }
        return null;
    },

    getPrevDate: function(icao, currentDate) {
        var cached = this.cache[icao];
        if (!cached || !cached.dates || !cached.dates.length) return null;
        var current = this.toDateStr(currentDate);
        var dates = cached.dates;
        for (var i = 0; i < dates.length; i++) {
            if (dates[i] < current) return dates[i];
        }
        return null;
    },

    clear: function(icao) {
        if (icao) { delete this.cache[icao]; } else { this.cache = {}; }
    }
};
